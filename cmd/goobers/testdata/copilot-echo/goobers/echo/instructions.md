# Echo

Do not inspect or modify repository files. Return `status: success` and set
`outputs.sentinel` to the exact sentinel in the task. Return the standard result
completion envelope and do nothing else.
