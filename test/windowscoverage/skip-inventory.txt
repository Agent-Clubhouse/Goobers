# Windows test-surface skip inventory (#2031). Every package in this module
# must appear either as a go-test target in .github/workflows/ci.yml's
# windows-smoke job, or here, with a reason — enforced by
# `make windows-coverage` (test/windowscoverage), which fails the build on any
# package accounted for by neither, on any package accounted for by both, and
# on any entry here for a package that no longer exists. This makes the
# untested-on-Windows set a reviewed, enumerated, drift-checked artifact
# instead of an accident of the allowlist's history.
#
# Format: one entry per line, `<import path> # <reason>`. The overwhelming
# majority below share one reason: not yet individually audited for
# Windows-specific behavior, with no known OS-conditional code, and (for
# anything the implementation workflow's daemon actually exercises) indirect
# coverage from test/windowsvalidate's real end-to-end daemon walk. A handful
# of entries have a sharper, specific reason — those are genuine, currently
# real gaps (an existing _unix_test.go with no Windows counterpart), not just
# unaudited.
github.com/goobers/goobers # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/api/integrity # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/api/schemas # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/api/validate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/cmd/config-sync # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/cmd/goober-runtime # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/cmd/operator # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/cmd/scheduler # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/config-examples # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/adoauth # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/agentkit # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/apicontract # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/apicontract/cmd/generate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/app # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/authoring # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/backlog # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/bootstrap # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/boundedagg # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/boundedwait # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/capability # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/clidocs # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/configdiff # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/configsource # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/configsync # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/daemonstate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/desktopnotify # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/dslmigrate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/engine # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/externaltelemetry # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/externaltelemetry/adx # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/failfirst # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/fieldpredicate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/gate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/githubapp # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/gooberassets # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/gooberruntime # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/harness # process_session_test.go is //go:build unix only, with no windows counterpart -- the harness process-session lifecycle has zero Windows-specific coverage, a genuine gap tracked here rather than silently left off the allowlist.
github.com/goobers/goobers/internal/instancefixture # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/invoke # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/iossimulator # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/k8spreflight # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/labelpredicate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/localscheduler # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/mcpconfig # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/mergepolicy # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/oidcauth # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/operator # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/platform/lock # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/platform/safeopen # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/procenv # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/providerfixture # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/providersnapshot # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/providerstage # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/readprobe # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/runcontrol # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/runner # run_unix_test.go is //go:build unix only, with no windows counterpart -- some runner behavior has zero Windows-specific coverage, a genuine gap tracked here rather than silently left off the allowlist.
github.com/goobers/goobers/internal/runnercap # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/sandbox # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/scheduler # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/secretstore # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/selfupdate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/service # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/signals # signals_test.go is //go:build unix only, with no windows counterpart -- the package's actual signal-dispatch behavior has zero Windows coverage, a genuine gap (not yet fixed by this pass; a real Windows-specific test needs OS-appropriate signal semantics, tracked here rather than silently left off the allowlist).
github.com/goobers/goobers/internal/speechnotify # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/supportmatrix # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/telemetry # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/telemetry/query # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/telemetry/retention # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/telemetry/rollup # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/temporaltest # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/testdep # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/toolchain # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/tutorguard # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/version # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/webhook # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/winsvc # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/workerhost # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/workflow # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/workflow/internal/model # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/workflow/v_current # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/internal/workflow/v_next # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/packaging # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/providers # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/release # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/release/notes # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/samples # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/telemetryconnector/v1alpha1 # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/telemetryconnector/v1alpha1/contracttest # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/benchworkcopy # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/ci # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/config # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/configvalidate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/coveragegate # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/deadcode # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/dslauthor # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/e2e # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/environmentresolver # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/hermetic # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/integration # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/nophonehome # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/providerfixtures # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/providers # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/scale # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/shippedworkflows # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/stress # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/testtiming # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
github.com/goobers/goobers/test/windowscoverage # the coverage-audit tool itself: a pure go-list/text-parsing check with no OS-conditional behavior, tested by its own main_test.go in the standard Linux unit-test tier, not Windows-specific runtime behavior.
github.com/goobers/goobers/test/workflowupgrade # not yet audited for Windows-specific behavior beyond the go build/go vet windows/amd64 compile check; no known OS-conditional code, and any daemon-lifecycle behavior it participates in is exercised indirectly by test/windowsvalidate's real daemon walk (#2031)
