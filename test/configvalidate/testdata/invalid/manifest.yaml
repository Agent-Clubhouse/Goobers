apiVersion: goobers.dev/v1alpha1
kind: Manifest
metadata:
  name: invalid-fixture
spec:
  instance:
    name: invalid-fixture
    environment: test
  connections:
    - name: repo-token
      type: repo
      provider: github
      secretRef:
        name: repo-token
  gaggles:
    - example
