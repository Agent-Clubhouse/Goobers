apiVersion: goobers.dev/v1alpha1
kind: Manifest
metadata:
  name: invalid-fixture
  annotations:
    goobers.dev/allow-preview-features: "true"
spec:
  instance:
    name: invalid-fixture
    environment: dev
  connections:
    - name: repo-token
      type: repo
      provider: github
      secretRef:
        name: repo-token
  gaggles:
    - example
