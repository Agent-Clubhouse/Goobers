# Keep the image build context lean and deterministic: the Dockerfile builds
# the Portal in its own stage, so VCS state and local build artifacts never
# belong in the context.
.git
.clubhouse
bin/
dist/
stress-results/
coverage.out
portal/node_modules/
internal/portalassets/dist/
