apiVersion: goobers.dev/v1alpha1
kind: Workflow
dslVersion: "2.0"
metadata:
  name: deploy
spec:
  gaggle: beta
  displayName: Beta deploy
  triggers:
    - type: manual
  start: deploy
  tasks:
    - name: deploy
      type: deterministic
      goal: Deploy beta.
      run:
        command: ["true"]
