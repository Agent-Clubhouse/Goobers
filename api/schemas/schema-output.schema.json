{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://goobers.dev/schemas/schema-output.schema.json",
  "title": "SchemaOutput",
  "description": "Release-pinned schema discovery emitted by goobers schema.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "version", "commit", "dslVersion"],
  "properties": {
    "schemaVersion": { "const": "goobers.dev/schema/v1", "description": "Version of the schema-command output contract." },
    "version": { "type": "string", "minLength": 1, "description": "Goobers build version that embedded this schema." },
    "commit": { "type": "string", "minLength": 1, "description": "Source commit of the Goobers build that embedded this schema." },
    "dslVersion": { "type": "string", "minLength": 1, "description": "Current workflow DSL version of the producing build." },
    "kind": { "type": "string", "minLength": 1, "description": "Selected embedded schema kind." },
    "kinds": {
      "type": "array",
      "description": "All schema kinds embedded in the producing build.",
      "items": { "type": "string", "minLength": 1 }
    },
    "schema": { "type": "object", "description": "The selected canonical JSON Schema, preserved byte-for-byte from the embedded source." }
  },
  "oneOf": [
    {
      "required": ["kind", "schema"],
      "not": { "required": ["kinds"] }
    },
    {
      "required": ["kinds"],
      "not": {
        "anyOf": [
          { "required": ["kind"] },
          { "required": ["schema"] }
        ]
      }
    }
  ]
}
