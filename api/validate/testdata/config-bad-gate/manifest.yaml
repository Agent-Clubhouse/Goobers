apiVersion: goobers.dev/v1alpha1
kind: Manifest
metadata:
  name: gate-instance
  annotations:
    goobers.dev/allow-preview-features: "true"
spec:
  instance:
    name: gate
    environment: dev
  gaggles:
    - web
