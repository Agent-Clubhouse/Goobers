apiVersion: goobers.dev/v1alpha1
kind: Manifest
metadata:
  name: compile-bad-instance
  annotations:
    goobers.dev/allow-preview-features: "true"
spec:
  instance:
    name: compile-bad
    environment: dev
  gaggles:
    - web
