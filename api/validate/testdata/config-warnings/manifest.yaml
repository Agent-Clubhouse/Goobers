apiVersion: goobers.dev/v1alpha1
kind: Manifest
metadata:
  name: warning-instance
  annotations:
    goobers.dev/allow-preview-features: "true"
spec:
  instance:
    name: warning-instance
    environment: dev
  gaggles:
    - web
