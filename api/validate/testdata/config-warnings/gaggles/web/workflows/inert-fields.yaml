apiVersion: goobers.dev/v1alpha1
kind: Workflow
dslVersion: "2.0"
metadata:
  name: inert-fields
spec:
  gaggle: web
  triggers:
    - type: schedule
      schedule: "@hourly"
  start: build
  tasks:
    - name: build
      type: deterministic
      goal: Build the project.
      run:
        command: ["true"]
      expectedOutputs:
        - artifact
